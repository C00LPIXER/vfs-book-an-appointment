# secrets & data — never commit
# all runtime data & secrets (accounts, passwords, proxies, cool-off state)
data/
state/*.json
state/*.db
state/*.log
state/
state/screenshots/
state/otp.txt
documents/

# browser profiles (contain live sessions/cookies)
*-profile/
browser-profile/
public-profile/
whatsapp-profile/

# python
.venv/
__pycache__/
*.pyc
*.egg-info/

# misc
*.log

# cloned reference repos (research only, not ours)
research/
